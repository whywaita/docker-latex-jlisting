box: wercker/wercker-labs-docker
build:
  steps:
    - script:
      name: docker build
      code: docker build -t whywaita/docker-latex-jlisting .
    - script:
      name: docker test
      code: docker run -it whywaita/docker-latex-jlisting /tmp/spec.sh
